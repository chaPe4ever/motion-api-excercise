# Ensure PostgreSQL search_path is set for schema isolation
# The search_path is configured via connection options in settings.py
# This file is kept for potential future use but currently relies on settings.py configuration
